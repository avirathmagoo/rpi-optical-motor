# /etc/dhcpcd.conf addition — append these lines to your existing file
# Sets static IP on the Ethernet interface for direct Arduino connection

interface eth0
static ip_address=192.168.10.1/24
static routers=
static domain_name_servers=
nolink
